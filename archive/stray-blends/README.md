# stray blends

Files that were saved into a working folder they do not belong to. Archived
rather than deleted (LAW #0.6 — a dead end is data, and the artefact is what
lets you RE-RUN when a premise changes).

## 2026-08-27-Untitled.blend

- **Was:** `jobs/wang-meng/marks/Untitled.blend`, 7.5 MB, saved 2026-08-27 07:14.
- **Why moved:** `marks/` holds marking scenes built by
  `tools/blender-mark-scene.py`, all of which are named for the zone they mark
  (`scroll-marks.blend`, `z3w-marks.blend`). An `Untitled.blend` there is a
  manual save that landed in the wrong directory, and it is 79x the size of
  every real marking scene.
- **Contents: UNVERIFIED.** The file is zstd-compressed and was not opened.
  Its timestamp is the morning after the 2026-08-26 blender-live session
  (donut tutorial, geometry-nodes sprinkles), which makes that the likely
  origin — but that is INFERENCE FROM A DATE, not a reading of the file.
  Open it in Blender before believing anything about it.
- **Nothing references it.** No script, config or claim in the repo names it.
